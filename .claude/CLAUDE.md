# 작업 플로우 (게임 기능 1건 기준)

1. 사람이 구글독스에 기획서를 작성한다.
2. implementer가 기획서를 읽고 다음을 정리해 사람에게 확인을 요청한다.
   - 엣지케이스 및 구현 방향
   - 기획서의 핵심 메커니즘과 관련된 실제 게임 레퍼런스 (예: "조작감" 기획이면
     셀레스트·할로우 나이트 등이 어떻게 구현했는지 웹 검색으로 찾아 요약)
   - 레퍼런스는 2~3개 이내로, 기획서 맥락에 직접 관련된 것만 포함한다.
   (유니티 코드/씬 구현이 필요 없는 작업, 예: 문서 정리는
   리드 세션이 직접 처리한다.)
3. 사람이 확인/승인하면 implementer가 구현을 시작한다.
4. implementer가 Unity MCP로 자체 테스트(논리 검증 + 필요시 스크린샷)까지
   마치고 커밋한다.
5. implementer가 완료 보고를 리드에게 전달하고, 리드는 그 보고를 그대로
   사람에게 전달한다.
6. 사람이 최종 확인(특히 시각적 완성도)을 한다.

7. 세션 중 설계/구현 결정이 나오면 (기획서와 코드 간 차이, 방향 변경 등)
   리드가 "이거 기록할까요?"라고 확인을 요청한다. 승인하면 해당 문서의
   "구현 참고" 섹션에 한 줄로 추가한다. 자동으로 기록하지 않는다.

# 브랜치 규칙

세션 시작 전 반드시 브랜치를 먼저 생성한다.

- 이슈 작업: `feature/issue-{번호}` (여러 개면 `feature/issue-12-14`)
- 정리/리팩토링: `chore/{내용}` (예: `chore/folder-cleanup`)
- main에 직접 커밋하지 않는다.

# 기획서 저장소

- 모든 최신/확정된 버전의 기획서는 **ThreadVault** (Obsidian)에 저장된다.

# 문서 작성 규칙

1. 사용자가 직접 쓴 텍스트는 원문을 그대로 유지한다. 수정·정제·교정하지 않는다.
2. 새로 작성하는 문서의 말투는 사용자의 스타일에 맞춘다.

# 협업 규칙 (리드 에이전트용)

1. 서브에이전트가 사용자에게 확인이 필요한 질문을 하면, 리드는 절대 대신 판단하거나
   "합리적인 기본값으로 진행"하도록 지시하지 않는다. 그 질문을 그대로 사용자에게 전달하고,
   사용자의 답을 받은 뒤에만 서브에이전트를 재개시킨다.

2. 확정된 기획서가 명시적으로 주어지기 전까지는 구현(코드 작성)을 시작하지 않는다.
   기획서 안에서도 아직 논의/디테일 조정 중인 부분이 있다면, 그 부분은 구현하지 않고
   사용자에게 먼저 확인을 요청한다.

3. 지금 작업 중인 기획서의 스코프를 벗어나는 질문이나 제안은 하지 않는다.
   스코프 외 사항은 사용자가 명시적으로 다시 논의하자고 하기 전까지 언급하지 않는다.

4. Unity MCP 도구 승인 기준:
   - 자동 승인: GameObject/컴포넌트 생성 및 추가, 속성(위치/값 등) 변경
   - 항상 확인: 무언가를 삭제·제거하는 모든 동작(GameObject 삭제, 컴포넌트 제거,
     씬 삭제/전환 등), 대량 일괄 작업
   - 이 규칙은 리드 세션과 서브에이전트 모두에 동일하게 적용된다.
