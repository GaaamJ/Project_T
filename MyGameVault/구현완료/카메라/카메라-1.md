# 카메라 #1

## 0. 개요

기존에는 카메라의 orthographic size와 방을 손수 배치해 UI가 들어갈 여백을 만들었다. 이 방법은 자연스럽지 않아, 게임 월드는 RenderTexture로 렌더링한 뒤 UI 안에 독립적으로 배치하기로 한다.

또한 플레이어가 방과 방을 이동할 때, 카메라가 이동한 방 위치로 전환된다.

## 1. 프로토타이핑

**< 체크리스트 >**
- [x] RenderTexture 전환 후에도 기존과 동일한 화면 구성으로 보이는지 확인한다.
- [x] 방이 전환될 때 카메라가 의도한 위치로 정확히 바뀌는지 확인한다.

**< 구현 >**
- World Camera의 Target Texture를 RenderTexture로 설정
- RenderTexture 해상도/Filter Mode를 PPU(16) 기준에 맞춤
- Canvas 안 RawImage로 RenderTexture 표시, Aspect Ratio Fitter 적용
- 방 전환 트리거(EntryDoor 등) 시 카메라 위치를 다음 방으로 전환하는 로직 추가
  - Cinemachine 사용

**< 구현 로그 >**
- FrameContainer의 Ratio는 반드시 16:9를 준수해야 함. (아니면 게임 화면이 찌그러져 보임)
- Display 1 No cameras rendering은 무시해도 됨.
- Cinemachine
  - Orthographic size = 11.25 ((보여주고 싶은 세로 화면 유닛 수) / 2)
  - transform.z = -10
