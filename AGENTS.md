# Codex 작업 규칙

## 공통 규칙

- 작업 전에 `.claude/CLAUDE.md`를 읽고 작업 플로우, 브랜치, 문서 작성, 협업 규칙을 따른다. 해당 파일을 공통 규칙의 기준으로 사용하며 이 문서에 복제하지 않는다.
- 기존 Claude Code 작업 흐름과 `.claude/` 설정을 유지한다.
- `ThreadVault`는 구현된 현재 동작을 설명한다. 사용자 스펙 원문, 미구현 계획, 목표·체크리스트는 이슈에 보존한다.

## 역할과 진행 순서

- Codex 메인 세션은 리드 역할이다. 사용자의 변경 스펙과 합의 과정의 수정은 이슈에 반영한다. 구현·검증 후 현재 동작을 ThreadVault에 반영한다. 문서만 다루는 작업은 리드가 직접 처리한다.
- 구현이 필요한 경우 implementer 서브에이전트에 `.claude/agents/implementer.md`와 해당 이슈의 승인된 스펙 및 현재 동작 기획서를 읽도록 지시한다.
- implementer는 엣지케이스, 구현 방향, 관련 게임 레퍼런스 2~3개 이내를 정리해 사용자에게 확인받는다. 질문은 리드가 대신 결정하지 않고 사용자에게 전달한다. 승인 후 구현, Unity MCP 자체 테스트, 커밋을 진행한다.
- 구현 완료 보고 후 별도의 verifier 서브에이전트에 `.claude/agents/verifier.md`와 해당 이슈의 승인된 스펙·완료 기준 및 현재 동작 기획서를 읽도록 지시한다. 자동 검증 결과, 사람 확인 체크리스트, 문제 재현 방법을 받아 사용자에게 전달한다.
- 서브에이전트의 도구·모델 이름은 Claude 전용 설정을 그대로 호출하지 않고 현재 Codex 환경에서 제공되는 기능에 맞춘다. 역할과 승인 순서는 유지한다.

## Unity와 Git

- Unity MCP 변경 작업 전 현재 인스턴스와 에디터 상태를 확인한다. 컴파일 중이거나 도구 실행 준비가 안 된 경우 준비 상태를 먼저 확인한다.
- Unity MCP 삭제·제거, 씬 전환, 대량 작업의 확인 기준은 `.claude/CLAUDE.md`를 따른다.
- 작업 시작 시 공통 브랜치 규칙에 맞는 브랜치를 만든다. 기존 사용자 변경은 보존하고 작업 대상 파일만 선택적으로 커밋한다.
- 새 파일은 `git diff`만으로 확인하지 않고 직접 읽기와 `git status`로 확인한다.
